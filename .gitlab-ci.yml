# Auto-generated by forge ci init
# 
# This pipeline is optimized based on your build graph:
# - Parallel execution where possible
# - Cache configuration enabled
# - Artifact sharing between jobs
# - Remote cache support if configured

stages:
  - stage-0

variables:
  FORGE_CACHE_ENABLED: "true"

build:
  stage: stage-0
  timeout: "30" minutes
  script:
    - cargo build --release
  cache:
    key: "$CI_COMMIT_REF_SLUG-build"
    paths:
      - .forge/cache
  artifacts:
    paths:
      - target/release/my_app
test:
  stage: stage-0
  timeout: "30" minutes
  needs:
    - build
  script:
    - cargo test
  cache:
    key: "$CI_COMMIT_REF_SLUG-test"
    paths:
      - .forge/cache

