# Dịch Vụ Phân Tán Go (`go/`) — ĐÃ LÊN KẾ HOẠCH, CHƯA CÀI ĐẶT

> ⚠️ **Trạng thái:** Các dịch vụ Go được mô tả trong các phiên bản trước của
> trang này (coordinator, worker gateway, tự động co giãn Kubernetes, P2P mesh,
> telemetry) **không tồn tại trong repo này** — hiện không có thư mục `go/`.
>
> Trang này chỉ là bản ghi chú giữ chỗ cho thiết kế tương lai. Thực thi phân tán
> hiện tại do các crate Rust `fish-worker` và `fish-remote-cache`
> (HTTP/JSON thuần) đảm nhiệm. Xem [ROADMAP.md](ROADMAP.md).
