# Go 分散式服務系統 (`go/`) — 規劃中，尚未實作

> ⚠️ **狀態說明：** 本頁早期版本描述的 Go 服務（coordinator、worker gateway、
> Kubernetes 自動伸縮、P2P mesh、telemetry）**在本儲存庫中並不存在** —— 目前沒有
> `go/` 目錄。
>
> 本頁僅作為未來設計的佔位說明。目前的分散式執行由 Rust crate
> `fish-worker` 與 `fish-remote-cache`（純 HTTP/JSON）實現。
> 詳見 [ROADMAP.md](ROADMAP.md)。
