{
  "name": "@fish-demo/web-frontend",
  "version": "0.1.0",
  "private": true,
  "description": "TypeScript frontend of the polyglot demo; consumes the TaskEvent contract owned by py-worker.",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "start": "node dist/index.js"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0"
  }
}
